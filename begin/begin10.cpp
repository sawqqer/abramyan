#include <bits/stdc++.h>

int main() { 
    using namespace std; 
    int a, b;
    cin >> a >> b;
    cout << "Sum: " << (a+b) << '\n' << "Difference: " << (a-b) << '\n' << "Product: " << a*b << '\n' << "Quotient: " << (double)a/b; 
    return 0;
}