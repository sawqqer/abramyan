#include <bits/stdc++.h>
#define fast_io ios_base::sync_with_stdio(0); cin.tie(0); cout.tie(0)

int main() { 
    using namespace std;  //Ax + B = 0  ax = -b   x = -b/a
    double a, b;
    cin >> a >> b;
    cout << -b/a;
	return 0;   
}